rem This won't work if:
rem 1. Chrome is not installed,
rem 2. Chrome's path is not part of the "PATH" environment variable, or
rem 3. any Chrome window is already running without the "--allow-file-access-from-files" switch.

chrome --allow-file-access-from-files